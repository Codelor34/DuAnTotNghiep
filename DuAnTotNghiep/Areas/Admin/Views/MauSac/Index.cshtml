@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@model IEnumerable<DuAnTotNghiep.Models.MauSac>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mausac)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Mausac)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID_MauSac">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ID_MauSac">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID_MauSac">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
